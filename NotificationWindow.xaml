<Window x:Class="WaterReminder.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Width="360"
        Height="220"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        KeyDown="Window_KeyDown">
    <Window.Resources>
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="24">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Opacity" Value="0.92" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Opacity" Value="0.78" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Chrome" Property="BorderThickness" Value="2" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="OuterShell"
            CornerRadius="28"
            Padding="1">
        <Border.Effect>
            <DropShadowEffect x:Name="ShadowEffect"
                              BlurRadius="28"
                              ShadowDepth="8"
                              Opacity="0.25" />
        </Border.Effect>
        <Border x:Name="CardBorder"
                CornerRadius="27"
                Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Border x:Name="BadgeBorder"
                            Width="44"
                            Height="44"
                            CornerRadius="22"
                            VerticalAlignment="Top">
                        <Viewbox Width="24" Height="24">
                            <Canvas Width="40" Height="52">
                                <Path Fill="White"
                                      Data="M20,0 C9,13 0,21 0,31 C0,42 9,51 20,51 C31,51 40,42 40,31 C40,21 31,13 20,0Z" />
                            </Canvas>
                        </Viewbox>
                    </Border>

                    <StackPanel Grid.Column="1"
                                Margin="14,2,8,0">
                        <TextBlock x:Name="TitleText"
                                   FontSize="19"
                                   FontWeight="SemiBold" />
                        <TextBlock x:Name="SubtitleText"
                                   Margin="0,8,0,0"
                                   FontSize="13"
                                   TextWrapping="Wrap" />
                    </StackPanel>

                    <Button x:Name="CloseButton"
                            Grid.Column="2"
                            Width="44"
                            Height="44"
                            Margin="10,0,0,0"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Click="CloseButton_Click"
                            Focusable="True"
                            Style="{StaticResource CloseButtonStyle}">
                        <Viewbox Width="24" Height="24">
                            <Canvas Width="24" Height="24">
                                <Path x:Name="CloseGlyph"
                                      StrokeThickness="4.2"
                                      StrokeStartLineCap="Round"
                                      StrokeEndLineCap="Round"
                                      Data="M4,4 L20,20 M20,4 L4,20" />
                            </Canvas>
                        </Viewbox>
                    </Button>
                </Grid>

                <Border x:Name="BodyCard"
                        Grid.Row="1"
                        Margin="0,18,0,18"
                        Padding="16"
                        CornerRadius="18"
                        Opacity="0.95">
                    <StackPanel>
                        <TextBlock x:Name="BodyText"
                                   FontSize="15"
                                   FontWeight="Medium" />
                        <TextBlock x:Name="HintText"
                                   Margin="0,10,0,0"
                                   FontSize="12"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </Border>

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock x:Name="NextTimeText"
                               VerticalAlignment="Center"
                               FontSize="12" />

                    <Button x:Name="DrinkButton"
                            Grid.Column="1"
                            MinWidth="110"
                            Padding="18,10"
                            Click="DrinkButton_Click"
                            FontSize="14"
                            FontWeight="SemiBold" />
                </Grid>
            </Grid>
        </Border>
    </Border>
</Window>
